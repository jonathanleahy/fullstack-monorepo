{
  "name": "fullstack-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "playbook",
    "design-tokens",
    "shared"
  ],
  "scripts": {
    "dev": "./scripts/dev-all.sh",
    "stop": "./scripts/stop-all.sh",
    "build": "./scripts/build-all.sh",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "precommit": "./scripts/precommit-check.sh"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
